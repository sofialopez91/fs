import { questionInt } from "readline-sync";
import as * rls from "readline-sync";

let num : number , linea: string;

let numero1: number= rls.questionInt (" ingrese numero 1 ");
let numero2: number = rls.questionInt ("ingrese numero 2 ");

let opcionMenu:number = rls.questionInt(" ingrese 1 para sumar ,2 para restar,culquier otra tecla para salir");
 
if (opcionMenu==1){
    for num =
}